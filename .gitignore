# OAuth / Google Cloud secrets — never commit these
client_secret*.json
*credentials*.json
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
node_modules
package.json
package-lock.json

# AI Literacy Hub PDF build — the cached Google Fonts woff2 files and the
# --png layout proofs. The PDFs themselves ARE committed; these are not.
AI Literacy Hub/.fontcache/
AI Literacy Hub/*.png
